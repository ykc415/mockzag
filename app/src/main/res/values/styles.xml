<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.MaterialComponents.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>\
        <item name="android:listDivider">@drawable/divider</item>
    </style>

    <style name="ToolbarStyle" parent="@style/Widget.AppCompat.Toolbar.Button.Navigation">
        <item name="contentInsetStart">0dp</item>
        <item name="android:contentInsetStart">0dp</item>
    </style>

    <style name="SodaChipChoice" parent="@style/Widget.MaterialComponents.Chip.Choice">
        <item name="chipBackgroundColor">@color/soda_background_color_chip_state_list</item>
        <item name="android:textColor">@color/soda_text_color_chip_state_list</item>
        <item name="android:checkable">true</item>
        <item name="android:focusable">true</item>
        <item name="android:clickable">true</item>
        <item name="android:textSize">6dp</item>
    </style>

    <style name="PinkChipChoice" parent="@style/Widget.MaterialComponents.Chip.Choice">
        <item name="chipBackgroundColor">@color/pink_background_color_chip_state_list</item>
        <item name="android:textColor">@color/pink_text_color_chip_state_list</item>
        <item name="android:focusable">true</item>
        <item name="android:clickable">true</item>
    </style>

</resources>
